plugins {
    id 'java'
}

group 'com.kafka.demo'
version '1.0'

repositories {
    mavenCentral()
}

dependencies {

    implementation group: 'org.apache.kafka', name: 'kafka-clients', version: '3.0.0'

}

test {
    useJUnitPlatform()
}